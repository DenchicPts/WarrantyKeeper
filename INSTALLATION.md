# Пошаговая инструкция по установке WarrantyKeeper

## Шаг 1: Установка необходимого ПО

### 1.1. Установка JDK 17

1. Скачайте JDK 17 с официального сайта:
   - Oracle: https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html
   - OpenJDK: https://adoptium.net/

2. Установите JDK, следуя инструкциям установщика

3. Проверьте установку:
```bash
java -version
# Должно показать: java version "17.x.x"
```

### 1.2. Установка Android Studio

1. Скачайте Android Studio с официального сайта:
   https://developer.android.com/studio

2. Запустите установщик и следуйте инструкциям

3. При первом запуске Android Studio:
   - Выберите "Standard" установку
   - Дождитесь загрузки всех компонентов SDK

## Шаг 2: Распаковка проекта

1. Распакуйте архив `WarrantyKeeper.zip` в удобное место
   Например: `C:\Projects\WarrantyKeeper\`

## Шаг 3: Настройка Firebase

### 3.1. Создание проекта Firebase

1. Откройте браузер и перейдите на: https://console.firebase.google.com/

2. Войдите с вашим Google аккаунтом

3. Нажмите "Add project" (Добавить проект)

4. Введите название проекта: **WarrantyKeeper**

5. Отключите Google Analytics (для простоты, можно включить позже)

6. Нажмите "Create project" и дождитесь создания

### 3.2. Добавление Android приложения в Firebase

1. В консоли Firebase выберите ваш проект **WarrantyKeeper**

2. Нажмите на иконку **Android** (робот)

3. Заполните форму:
   - **Android package name**: `com.warrantykeeper` (ВАЖНО: точно как написано!)
   - **App nickname**: `WarrantyKeeper` (опционально)
   - **Debug signing certificate SHA-1**: (см. следующий шаг)

### 3.3. Получение SHA-1 сертификата

1. Откройте командную строку Windows (Win + R → cmd)

2. Выполните команду:
```bash
keytool -list -v -keystore "%USERPROFILE%\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android
```

3. В выводе найдите строку:
```
SHA1: 3B:6F:A2:12:... (длинная строка символов)
```

4. Скопируйте этот SHA1 и вставьте в поле Firebase

5. Нажмите "Register app" (Зарегистрировать приложение)

### 3.4. Скачивание google-services.json

1. Firebase предложит скачать файл `google-services.json`

2. Скачайте этот файл

3. **ВАЖНО:** Поместите файл в папку проекта:
   ```
   WarrantyKeeper/app/google-services.json
   ```

### 3.5. Включение Google Sign-In

1. В Firebase Console слева выберите **Authentication**

2. Перейдите на вкладку **Sign-in method**

3. Найдите **Google** в списке провайдеров

4. Нажмите на Google → **Enable** (Включить)

5. В поле "Project support email" выберите ваш email

6. Нажмите **Save** (Сохранить)

## Шаг 4: Открытие проекта в Android Studio

1. Запустите **Android Studio**

2. Выберите **File → Open**

3. Найдите и выберите папку `WarrantyKeeper`

4. Нажмите **OK**

5. Android Studio начнет синхронизацию Gradle - дождитесь завершения (может занять 5-10 минут)

6. Если появятся ошибки о недостающих SDK:
   - Нажмите на ссылку в ошибке
   - Согласитесь на установку недостающих компонентов

## Шаг 5: Подготовка устройства для тестирования

### Вариант A: Использование реального устройства (рекомендуется)

1. **Включите режим разработчика на Android устройстве:**
   - Перейдите в **Настройки → О телефоне**
   - Нажмите 7 раз на **Номер сборки**
   - Появится сообщение "Вы стали разработчиком"

2. **Включите отладку по USB:**
   - Вернитесь в **Настройки**
   - Найдите **Для разработчиков** (Developer options)
   - Включите **Отладку по USB** (USB debugging)

3. **Подключите устройство к компьютеру:**
   - Подключите телефон USB кабелем
   - На телефоне разрешите отладку по USB

4. **Проверка подключения:**
   - В Android Studio вверху должно появиться ваше устройство
   - Если не появилось, установите USB драйвера для вашего устройства

### Вариант B: Использование эмулятора

1. В Android Studio нажмите **Tools → Device Manager**

2. Нажмите **Create Device**

3. Выберите устройство (например, Pixel 5) → **Next**

4. Скачайте system image:
   - Выберите **Tiramisu** (API 33) или **UpsideDownCake** (API 34)
   - Нажмите **Download** и дождитесь загрузки
   - Нажмите **Next**

5. Нажмите **Finish**

6. Запустите эмулятор кнопкой Play

## Шаг 6: Запуск приложения

1. В Android Studio убедитесь, что устройство выбрано в верхней панели

2. Нажмите зеленую кнопку **Run** (Play) или нажмите **Shift + F10**

3. Приложение скомпилируется и установится на устройство

4. При первом запуске:
   - Нажмите "Войти через Google"
   - Выберите ваш Google аккаунт
   - Приложение готово к работе!

## Шаг 7: Первое использование

1. **Добавьте первый документ:**
   - Нажмите кнопку "+" внизу справа
   - Разрешите доступ к камере
   - Сфотографируйте чек или гарантийный талон
   - Выберите тип документа

2. **Проверьте работу OCR:**
   - Если выбрали "Гарантия", приложение попытается распознать:
     * Дату покупки
     * Срок гарантии
     * Название товара

3. **Настройте уведомления:**
   - Перейдите в Настройки (иконка шестеренки)
   - Включите уведомления
   - Выберите период напоминания (например, 7 дней)

## Возможные проблемы и решения

### Проблема: "google-services.json not found"

**Решение:**
- Убедитесь, что файл `google-services.json` находится точно в папке `app/`
- Путь должен быть: `WarrantyKeeper/app/google-services.json`

### Проблема: Google Sign-In не работает / "Developer Error"

**Решение 1:** Проверьте SHA-1
```bash
# Выполните в командной строке
keytool -list -v -keystore "%USERPROFILE%\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android

# Сравните SHA1 с тем, что в Firebase Console
```

**Решение 2:** Убедитесь, что Google Sign-In включен в Firebase:
- Firebase Console → Authentication → Sign-in method → Google → Enabled

**Решение 3:** Пересоздайте `google-services.json`
- Скачайте файл заново из Firebase Console
- Замените старый файл новым

### Проблема: Gradle sync failed

**Решение:**
- File → Invalidate Caches → Invalidate and Restart
- Подождите 2-3 минуты после перезапуска

### Проблема: Камера не работает

**Решение:**
- На реальном устройстве: убедитесь, что разрешили доступ к камере
- На эмуляторе: в настройках эмулятора включите камеру

### Проблема: OCR не распознает текст

**Это нормально!** ML Kit работает лучше с:
- Четким текстом
- Хорошим освещением
- Печатным текстом (не рукописным)

**Решения:**
- Сфотографируйте документ повторно с лучшим освещением
- Отредактируйте данные вручную после распознавания

## Дополнительная помощь

### Где искать логи ошибок:

1. В Android Studio внизу откройте вкладку **Logcat**
2. Выберите ваше приложение в фильтре
3. Найдите сообщения красного цвета (ошибки)

### Полезные команды для отладки:

```bash
# Проверка подключенных устройств
adb devices

# Очистка данных приложения
adb shell pm clear com.warrantykeeper

# Переустановка приложения
./gradlew installDebug
```

## Контакты

По вопросам установки и настройки:
- Email: support@warrantykeeper.app
- GitHub Issues: (если проект на GitHub)

---

**Поздравляем!** Вы успешно настроили и запустили WarrantyKeeper! 🎉
